<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
           "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dz.shop.Dao.DeliveryStatusDAO">
	<select id="list" resultType="com.dz.shop.Delivery.DeliveryStatus">
		select * from delivery_status
		order by 'no' desc;
	</select>
</mapper>