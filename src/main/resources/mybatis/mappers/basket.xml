<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
           "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dz.shop.Dao.BasketDAO">
	<select id="list" resultType="HashMap">
		select *
		from product_basket pb
				 left join product p on pb.product_no = p.no
			and pb.userId = '123';
	</select>
	<insert id="add">
		insert into product_basket(
			product_no, userId, cnt, register_date, update_date
		)values (
			#{product_no}, #{userId}, 1, now(), now()
		)
	</insert>
</mapper>